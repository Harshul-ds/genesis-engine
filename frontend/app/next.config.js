/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Your warnings about 'appDir' and 'optimizeCss' mean they are no longer
  // needed in this format for Next.js 14. We are removing them.
};

module.exports = nextConfig;
